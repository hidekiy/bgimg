#!/bin/sh

export PATH="/app/vendor/perl/bin:/app/vendor/perl-deps/bin:$PATH"
export PERL5OPT="-Mlocal::lib=/app/vendor/perl-deps"
starman="$(which starman)"
echo "PORT=$PORT, PATH=$PATH, PERL5OPT=$PERL5OPT, starman=$starman"
perl "$starman" --preload-app --port "$PORT" --workers 4
